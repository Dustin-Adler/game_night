%h5 Groups
%table
  %thead
    %tr
      %th Admin
      %th Group name
      %th Description
      %th{:colspan => "3"}
  %tbody
    - @groups.each do |group|
      %tr
        %td= group.admin.username
        %td= group.group_name
        %td= group.description
        %td= link_to 'Details', group